# MyComfyUI Application API の利用者設定例。
# このファイルをリポジトリ直下へ `.env` としてコピーし、値を環境に合わせて書き換える。
# `.env` は追跡対象外とする。設定値に API キーなどの秘密情報を置かない。

# 生成履歴とデータベースの保存先。
# 未設定の場合は OS 標準の利用者データ領域を使う。
# SQLite は <data_root>/db/mycomfyui.sqlite3 へ作成される。
MYCOMFYUI_DATA_ROOT=/home/example/.local/share/MyComfyUI
