# MyComfyUI 開発規約

本リポジトリで作業するagentの規約の正本。Codexは本ファイルを直接読み、Claude Codeは`CLAUDE.md`の参照を経由して本ファイルを読む。規約はここだけに書き、`CLAUDE.md`へ実体を置かない。

個人開発規約 (`~/.claude/CLAUDE.md`) を本リポジトリ向けに上書きする。ここに書かれていない事項は個人開発規約に従う。技術スタック、構成、実行コマンド、レビュー基準は対象に最も近いAGENTS.mdを優先する。

## 目的と完了

開発速度を優先し、必要な挙動を最小の差分で実現する。規約内の定型手順を省略・変更した場合だけ理由を1行報告する。以下の安全境界と、結果の正確な報告は省略しない。

依頼された成果物と完了条件を満たすまで進める。初期実装や計画の提示だけで終了しない。既に許可された範囲の修正は再確認しない。判断待ちが必要でも、独立して進められる作業は完了する。

## 安全境界

- セキュリティ懸念を発見したら、影響する実装・操作を止めて先に報告する。
- 破壊的操作（削除、force push、ブランチ削除など）は対象と影響範囲を示し、実行前に明示許可を得る。例外は後述のマージ後の後片付けに列挙した3つだけとする。
- `git commit --no-verify`と`-n`、squash commitとsquash mergeは禁止。
- MR/PR自己マージは「MR/PRの自己マージ」の条件を満たす場合に許可を求めずに行う。
- マージ後の後片付けは、マージ経路を問わず許可を求めずに続けて行う。対象は**worktree削除、マージ済みbranchのローカル・リモート削除、Issueのクローズの3つに限る**。前2つは個人開発規約の「破壊的操作（`rm -rf`、force push、ブランチ削除など）は実行前に対象と影響範囲を確認し、ユーザーの明示許可を得る」を、マージ済みPRの後片付けに限って上書きする。マージ済みの内容はmainに残るため、これらの削除では作業が失われないからである。Issueのクローズは破壊的操作に当たらず、上書きではなく運用として許可を求めない。未マージbranchの削除、force push、`rm -rf`は上書きの対象外とし、引き続き明示許可を得る。
- メインworking treeは直接編集しない。リポジトリ直下の`.worktree/<name>/`に`git worktree add`で作成する。`/tmp`は使わない。`.worktree/`は`.gitignore`か`.git/info/exclude`で除外する。
- マージ後はmain worktreeで`git fetch origin --prune`と`git pull --ff-only`を実行し、できなければ理由を報告する。

## MR/PRの自己マージ

**本リポジトリではPRの自己マージにユーザーの明示許可を要しない。** 個人開発規約の「MR/PRを自己マージしない（ユーザーがその場で明示許可したときのみ例外）」を上書きする。

個人利用のリポジトリであり、レビュー担当者が作成者以外に存在しないため、マージのたびに許可を求めると手が止まる。

自己マージする前に次を満たす。

- 受入基準として明示された検証が緑であること。省略した検証はPR本文かPRコメントへ明記する。
- 自己レビューと他者レビューを実施し、CRITICALとHIGHの指摘が残っていないこと。
- target branchと同期済みで、merge conflictが解消されていること。

他者レビューは独立した別agentによるレビューで代替する。作成者と同じ文脈を持たないagentへ差分を渡し、指摘をPRへ残す。他者レビューを実施できない場合はマージせず、理由と必要なreviewerを報告する。

次は上書きの対象外とし、引き続き個人開発規約に従う。

- squash commitとsquash mergeを行わない。マージはmerge commitで行う。
- `git commit --no-verify`と`-n`を使わない。
- 破壊的操作（`rm -rf`、force push、未マージbranchの削除など）の扱いは「安全境界」の規定に従う。マージ済みPRの後片付けだけは「安全境界」で許可不要へ上書きしてある。
- セキュリティ懸念を発見したら実装を止めて先に報告する。

## 応答

- 日本語で短く、結論から書く。他言語は明示要求時のみ。日本語とASCII間の空白、絵文字、固定の完了報告テンプレートは使わない。
- 根拠、変更、検証、未解決点から必要な情報だけ伝える。短さのために条件や未実施事項を落とさない。「原始人やめて」「通常モード」と指定されたら極端な短文を解除する。
- 日本語技術文書の作成・編集・レビューはjapanese-writingを使う。コード・設定本体、commit message、短いnoteは対象外。
- handoff・subagent用プロンプトにコードフェンスを含む場合、外側は4バッククォートにする。

## 開発と検証

### 開発サイクル

機能追加・修正は原則として次の順序で完了まで進める。レビューや修正だけで終了しない。

1. 目的、完了条件、制約、実装方針を計画する。
2. 計画をIssueへ反映し、IssueをSpecの正本として受入基準と実装計画を確定する。
3. IssueのSpecに基づくSpec-Driven Development(SDD)で実装する。
4. Draft MR/PRを作成する。
5. 自己レビューと、独立した別agentによる他者レビューを実施する。
6. 指摘を修正して両レビューを再実施し、修正事項がなくなるまで繰り返す。
7. 「MR/PRの自己マージ」の条件を満たしたらマージする。
8. cleanupとしてIssue、roadmap、main同期、マージ済みbranch・worktreeを整理する。

マージ可否の条件は「MR/PRの自己マージ」に集約する。branch・worktreeの削除は「安全境界」の規定に従う。

- 既存設計・命名・抽象を再利用する。無関係なリファクタ、テストの削除・無効化、ノイズ差分は禁止。新規依存は必要性と影響を確認する。
- テスト・型チェック・lint・TDDはユーザーが明示した場合だけ追加・実行する。明示された検証は対象リポジトリ指定のcheckを優先する。
- 最終差分で依頼範囲、回帰リスク、秘密情報混入を確認する。未実行の検証、失敗、環境制約は明記する。
- 失敗は再現と原因特定から直す。症状を隠す継ぎ足しや例外の握りつぶしで凌がない。設計変更が依頼範囲を超える場合だけ理由と案を示す。
- 失敗した試みは、何を・なぜ・結果をIssue/MRのnoteか引き継ぎに残す。

## 調査と判断

- 対象を読んでから答え、推測で断定しない。探索は`rg`、シンボル検索、差分から必要範囲に絞る。独立した読取りは並列化する。
- 既読資料は変更や情報不足がある場合だけ再読する。長時間処理は完了通知を優先し、出力と状態確認を必要最小限にする。
- 未知または更新頻度の高いライブラリ/APIは実装前に公式資料を確認する。
- ログ共有は失敗コマンド、exit code、主要エラー、該当箇所、expected/actualに絞り、秘密情報を含めない。
- 外部挙動・セキュリティ・互換性・データ保持が選択で変わる場合、または文書と実装が明確に矛盾する場合だけ質問する。他は安全な既定値で進める。質問は1件ずつ、必要なら推奨案と理由・代替案を示す。
- 推論レベルの変更が役立つ場合だけ理由付きで提案する。提案だけで設定変更や作業停止をしない。実測なしに効率を断定せず、同じ提案を繰り返さない。

## 引き継ぎ

自動圧縮や工程の区切りだけを理由に作業を中断しない。明示依頼、または文脈欠落で判断や作業継続が難しくなった場合にhandoffを使う。既読資料、決定理由、作業場所・ブランチ、未コミット変更、失敗済み試行、残作業、次の1手を必要に応じて残す。既存文書は参照する。引き継ぎのための無指示commitや処理停止はしない。

## ホスティングと保守

- 本リポジトリのremoteは`github.com`のため`gh`を使う。GitHubのWeb取得も`gh`で行う。`gitlab.heroz.co.jp`は`glab`を使う。
- commit・push・MR/PR作成は明示依頼または適用skillの範囲内のみ。GitLabの一巡はgitlab-auto-cycle、個別作業は各GitLab skillを使う。
- 反復した失敗・レビュー指摘だけを永続ルール化する。追記前に既存指示の重複・矛盾・広すぎる適用条件を調べる。本リポジトリ固有のルールは本ファイル、長い手順はskillや参照資料、機械判定できる規則はscript・hook・CIへ置く。
