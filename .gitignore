.worktree/
docs/issue/
docs/mr/

# 利用者ごとの設定。追跡対象外とし、example をコピーして使う。
.env

# Python
__pycache__/
*.py[cod]
.venv/
.ruff_cache/
.pytest_cache/

# Node.js
node_modules/
apps/web/dist/

# Tauri
# Rust のビルド成果物。Cargo.lock は追跡する。
apps/desktop/src-tauri/target/
apps/desktop/src-tauri/gen/
# PyInstaller で固めた sidecar の置き場。実行ファイルは配布時に作る。
apps/desktop/src-tauri/binaries/

# Playwright MCP の作業出力
.playwright-mcp/

# 常駐運用のログ出力先(docs/operations/remote-gpu-host.md 手順3)。
# リポジトリ直下だけを対象にする。配下の別ディレクトリのlogsまで巻き込まない。
/logs/
