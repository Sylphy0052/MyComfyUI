# MyComfyUI リポジトリ規約

個人開発規約 (`~/.claude/CLAUDE.md`) を本リポジトリ向けに上書きする。ここに書かれていない事項は個人開発規約に従う。

## MR/PR の自己マージ

**本リポジトリでは PR の自己マージにユーザーの明示許可を要しない。** 個人開発規約の「MR/PR を自己マージしない (ユーザーがその場で明示許可したときのみ例外)」を上書きする。

個人利用のリポジトリであり、レビュー担当者が作成者以外に存在しないため、マージのたびに許可を求めると手が止まる。

自己マージする前に次を満たす。

- 受入基準として明示された検証が緑であること。省略した検証は PR 本文か PR コメントへ明記する。
- 自己レビューを実施し、CRITICAL と HIGH の指摘が残っていないこと。
- target branch と同期済みで、merge conflict が解消されていること。

次は上書きの対象外とし、引き続き個人開発規約に従う。

- squash commit と squash merge を行わない。マージは merge commit で行う。
- `git commit --no-verify` と `-n` を使わない。
- 破壊的操作 (`rm -rf`、force push、ブランチ削除など) は実行前に対象と影響範囲を示し、明示許可を得る。
- セキュリティ懸念を発見したら実装を止めて先に報告する。

マージ後は main worktree で `git fetch origin --prune` と `git pull --ff-only` を実行する。
